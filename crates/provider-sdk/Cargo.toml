[package]
name = "provider-sdk"
version = "0.1.0"
edition = "2021"
description = "Portable provider adapter contract for the NeoTavern Runtime Kernel (ТЗ §55, Фаза 7)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
