[package]
name = "neotavern-chat-viewport"
version = "0.1.0"
edition = "2021"
description = "Chat viewport virtualization and fling-continuous geometry remap (not compositor, not Android cutover)"
license = "AGPL-3.0"
publish = false

[lib]
name = "neotavern_chat_viewport"

[dependencies]
