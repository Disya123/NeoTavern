# Theme starter source

This directory is the source of truth for the downloadable
`apps/web/public/theme-starter.zip`.

After editing these files, run `pnpm theme:starter`. The generator creates a
deterministic ZIP without network access or third-party tools. The Theme SDK
contract test verifies that the archive and this directory remain identical.
